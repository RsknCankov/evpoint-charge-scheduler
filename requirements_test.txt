# Test dependencies for the EVPoint Charge Scheduler pytest suite.
#
# Pin PHACC only — it transitively pins homeassistant, pytest, and
# pytest-asyncio. Do NOT separately pin homeassistant/pytest/pytest-asyncio
# (resolver conflict; see .planning/research/STACK.md "What NOT to Use").
#
# FALLBACK PIN (3.13 / 0.13.300): the current PHACC (0.13.336 → HA 2026.6.0)
# requires Python >= 3.14, which is unavailable in this dev/CI environment
# (only Python 3.13 is present). Per 01-RESEARCH.md's documented fallback we
# pin PHACC 0.13.300 (HA 2025.12.2, Python >= 3.13). The integration uses only
# stable HA APIs, so HA 2025.12 vs 2026.6 makes no behavioural difference to
# the suite. Restore 0.13.336 + Python 3.14 once 3.14 is available.
pytest-homeassistant-custom-component==0.13.300
# PHACC 0.13.300 transitively pins freezegun==1.5.2, so an exact ==1.5.5 pin
# (which targets the 0.13.336 line) is a resolver conflict here. Use a range
# and let PHACC's transitive pin resolve it.
freezegun>=1.5,<2
